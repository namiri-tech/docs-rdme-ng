# DigiTax Nigeria API

## PDF export

For the reference:

Generate PDF using "Markdown PDF" VSCode extension. It references the config in `.vscode/settings.json` and `.vscode/markdown.css` files.

For the API Spec, run:

`npx apibake reference/openapi.yaml --title 'DigiTax Nigeria API Spec'`
